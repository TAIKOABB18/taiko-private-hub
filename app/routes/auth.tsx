import { Form, redirect, useActionData } from "react-router";
import type { Route } from "./+types/auth";

export function meta(){return [{title:"TAIKO PRIVATE HUB · Acceso"}];}
async function callAuth(request:Request,path:string,payload:Record<string,string>){const endpoint=new URL(`/api/auth/${path}`,request.url);return fetch(endpoint,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});}
export async function action({request}:Route.ActionArgs){const form=await request.formData(),intent=String(form.get("intent")||"login"),email=String(form.get("email")||"").trim().toLowerCase(),password=String(form.get("password")||""),name=String(form.get("name")||"").trim();const upstream=await callAuth(request,intent==="register"?"owner-register":"login",{email,password,name});const data=await upstream.json().catch(()=>({error:"auth_failed"})) as {error?:string;redirect?:string};if(!upstream.ok)return {ok:false,error:data.error??"auth_failed"};const headers=new Headers(),setCookie=upstream.headers.get("set-cookie");if(setCookie)headers.set("set-cookie",setCookie);throw redirect(data.redirect||"/",{headers});}
export default function Auth(){const result=useActionData<typeof action>();return <main className="min-h-svh bg-[#020b17] p-6 text-white"><div className="mx-auto grid max-w-md gap-8 pt-16"><header><h1 className="text-3xl font-bold">TAIKO PRIVATE HUB</h1><p className="mt-2 text-slate-400">Acceso seguro</p></header><section className="rounded-2xl border border-slate-700 bg-slate-900 p-6"><h2 className="mb-4 text-xl font-semibold">Iniciar sesión</h2><Form method="post" className="grid gap-3"><input type="hidden" name="intent" value="login"/><input name="email" type="email" required autoComplete="email" placeholder="Email" className="rounded-lg border border-slate-700 bg-slate-950 p-3"/><input name="password" type="password" required autoComplete="current-password" placeholder="Contraseña" className="rounded-lg border border-slate-700 bg-slate-950 p-3"/><button className="rounded-lg bg-blue-600 p-3 font-semibold">Entrar</button></Form></section><section className="rounded-2xl border border-slate-700 bg-slate-900 p-6"><h2 className="mb-1 text-xl font-semibold">Registro Administrador</h2><p className="mb-4 text-xs text-slate-400">Reservado exclusivamente a tramiteshbc@gmail.com</p><Form method="post" className="grid gap-3"><input type="hidden" name="intent" value="register"/><input name="name" required autoComplete="name" placeholder="Nombre" className="rounded-lg border border-slate-700 bg-slate-950 p-3"/><input name="email" type="email" required autoComplete="email" defaultValue="tramiteshbc@gmail.com" readOnly className="rounded-lg border border-slate-700 bg-slate-950 p-3"/><input name="password" type="password" minLength={12} maxLength={128} required autoComplete="new-password" placeholder="Contraseña · mínimo 12 caracteres" className="rounded-lg border border-slate-700 bg-slate-950 p-3"/><button className="rounded-lg bg-violet-600 p-3 font-semibold">Registrarme como Administrador</button></Form></section>{result&&!result.ok&&<p role="alert" className="rounded-lg border border-red-900 bg-red-950/50 p-3 text-sm text-red-200">{result.error}</p>}</div></main>;}
